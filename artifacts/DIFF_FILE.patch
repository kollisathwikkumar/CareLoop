--- /Users/chipichipi/Documents/CareLoop/src/data.ts (before)
+++ /Users/chipichipi/Documents/CareLoop/src/data.ts (after)
@@
-export const initialPatients = [seeded patient records]
-export const initialCommunications = [seeded communication records]
-export const initialTasks = [seeded task records]
-export const initialReschedules = [seeded reschedule records]
-export const initialNotifications = [seeded notifications]
-export const initialActivity = [seeded activity records]
+export const initialPatients: Patient[] = []
+export const initialCommunications: Communication[] = []
+export const initialTasks: Task[] = []
+export const initialReschedules: RescheduleRequest[] = []
+export const initialNotifications: Notification[] = []
+export const initialActivity: Activity[] = []
+export const staff: string[] = []

--- /Users/chipichipi/Documents/CareLoop/src/App.tsx (before)
+++ /Users/chipichipi/Documents/CareLoop/src/App.tsx (after)
@@
-Overview and analytics used hardcoded offsets, IDs, people, dates, chart series, and demo profile values.
+Overview and analytics derive counters from empty state and render zero/connection-ready empty states.
@@
-Header navigation overflowed into the search control at narrower desktop widths.
+Header navigation is clipped within its flex region and utilities keep a reserved width.
@@
-Demo portal/settings/template content was rendered as application data.
+Portal, settings, templates, and communication views wait for backend records/configuration.

--- /Users/chipichipi/Documents/CareLoop/src/styles.css (before)
+++ /Users/chipichipi/Documents/CareLoop/src/styles.css (after)
@@
-search control could cover the Analytics navigation label
+responsive desktop header rules prevent overlap and compact utilities at 981-1380px
@@
-no empty analytics visualization state
+clear empty chart messaging and neutral zero-status donut styling
